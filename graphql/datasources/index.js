const CustomerDS = require('./customer');
const AuthDS = require('./auth');
const AuctionDS = require('./auction');
const ProductDS = require('./product');

module.exports = {
  CustomerDS,
  AuthDS,
  AuctionDS,
  ProductDS,
};
